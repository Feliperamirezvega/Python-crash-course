#Firts Code Game.
# This is my firts Guess Game with Pyhton.

SecretNumber = 40 # The secret number to guess

guess = int(input("I'm thinking of a number! Try to guess the number I'm thinking of:"))
while SecretNumber > guess:
    print("Too low!") # if the number is too low, the program will give you this answer.
    guess = int(input("Guess again: "))
while SecretNumber < guess:
    print("Too high!") # if the number is too high, the program will give you this answer.
    guess = int(input("Guess again: "))
while SecretNumber == guess:
    print("That's it!, would you like to play again? (yes/no)")
    answer = input(" ")
    if answer == "yes":
        guess = int(input("I'm thinking of a number! Try to guess the number I'm thinking of:"))
    elif answer == "no":
        print("Thanks for playing!")
        break

